<!DOCTYPE html>
<html ng-app="app">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <div ng-controller="HelloController">
      <input type="" name="" value="" ng-model="hello"> {{ hello }}
      <ul>
        <li ng-repeat="name in names">{{ name.fname }}</li>
      </ul>
    </div>
    <div ng-controller="BotController">
      <select class="" name="" ng-model="sorter">
        <option value="word">word</option>
        <option value="response">response</option>
      </select>
        <select class="" name="" ng-model="sorterType">
          <option value="0">A-Z</option>
          <option value="1">Z-A</option>
        </select>
      <input type="text" name="" value="" ng-model="search">
      <table>
        <thead>
          <tr>
            <th>Word</th>
            <th>Response</th>
          </tr>
        </thead>
        <tbody>
          <tr ng-repeat="sentence in sentencesTable">
            <td><input type="text" name="" value="{{ sentences[$index].word }}" ng-model="sentence.word"></td>
            <td><input type="text" name="" value="{{ sentences[$index].response }}" ng-model="sentence.response"></td>
          </tr>
          <tr ng-show="result.length == 0">
            <td colspan=2>Aucun résultat à afficher</td>
          </tr>
        </tbody>
      </table>
    </div>
    <script src="/public/libs/angular.min.js" type="text/javascript"></script>
    <script type="text/javascript" src="/public/app/app.module.js"></script>
    <script type="text/javascript" src="/public/app/bot.controller.js"></script>
  </body>
</html>
